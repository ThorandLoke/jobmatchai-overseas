# 2026-04-12 日志

## ThorandLoke 个人主页
- 域名 `thorandloke.com` 未被注册，可注册
- 页面已完成（/tmp/thorandloke.html），包含：Hero、Team、Products、Skills、Contact
- 待部署到 Cloudflare Pages

---

## JobMatchAI 开发（晚间）

### Skill Gap 分析重构
**问题**：硬编码关键词匹配导致 Power BI 等技能被错误推荐

**修复**：
- ✅ 新增 `analyze_skill_gap_ai()` 函数 - 完全由 AI 驱动
- ✅ 移除 280 行硬编码关键词匹配代码
- ✅ Prompt 规则：只列出职位描述中明确提到的技能，禁止编造

**AI Prompt 关键规则**：
```
1. Only list skills in missing_skills if EXPLICITLY mentioned in job description
2. Do NOT guess or hallucinate skills
3. Focus on what job REQUIRES vs nice-to-have
4. Look for: tools, certifications, years, languages
```

### Bug 修复
1. **progressMessages 未定义** - 变量定义顺序错误（已修复）
2. **ip_geo 模块属性名错误** - `_xdb_content` 改为 `_CHINA_IP_RANGES`（原有bug，已修复）
3. **server.js 代理规则错误** - 所有请求被代理到后端（已修复，只代理 API）
4. **soft_skill_suggestions 未定义** - 变量未初始化（已修复）

### 开发环境
- 前端：`node server.js` 在 localhost:8080
- 后端：`python3 backend_main.py` 在 localhost:8000
- 前端文件：/JobMatchAI-Overseas/frontend/index-zh.html

### 架构说明
- **开发**：Node.js server.js 代理 API 到 localhost:8000
- **生产**：Cloudflare Worker 代理 API 到 Render.com
- 前端代码无需改动，使用相对路径 `fetch('/api')`

### 待测
- Groq API 配额已用完（100000 tokens），等待恢复
- Skill Gap 分析 - 待用真实职位测试

### 求职信问题（未完全解决）
- 问题：生成的求职信包含简历中没有的案例（瞎编）
- 原因：Prompt 示例误导 + 未明确要求使用简历内容
- 状态：需要进一步优化 prompt
